# For running node locally:
# MONGO_URI=mongodb://localhost:27017

# For running a node container that accesses the local machine's mongodb
# MONGO_URI=mongodb://host.docker.internal:27017

# For running a node container that accesses the mongo container
MONGO_URI=mongodb://admin:secret@mongo:27017/stocks_data?authSource=admin
DB_NAME=stocks_data